{
  "name": "mapkit-parser-server",
  "version": "1.0.0",
  "description": "Apple MapKit JavaScript parser server using Bun and Hono",
  "main": "index.ts",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts",
    "build": "bun build index.ts --outdir ./dist",
    "test": "bun test"
  },
  "keywords": ["mapkit", "apple", "parser", "bun", "hono", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}